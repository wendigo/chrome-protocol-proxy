builds:
  - binary: chrome-protocol-proxy
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64

archive:
  format: tar.gz
  replacements:
    amd64: x86_64
    darwin: macos
  files:
    - LICENSE
    - README.md
